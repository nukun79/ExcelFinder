<Window x:Class="ExcelFinder.ExcelEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Excel Editor" Height="720" Width="1200" MinHeight="520" MinWidth="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="FileInfoTextBlock" Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,8">
            <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="SaveButton" Content="저장" Width="100" Height="30" Margin="0,0,8,0" Click="SaveButton_Click"/>
                <Button Content="Diff" Width="100" Height="30" Margin="0,0,8,0" Click="DiffButton_Click"/>
                <Button Content="체크아웃" Width="100" Height="30" Margin="0,0,8,0" Click="CheckoutButton_Click"/>
                <Button x:Name="CheckinButton" Content="체크인" Width="100" Height="30" Margin="0,0,8,0" Click="CheckinButton_Click"/>
                <Button Content="리버트" Width="100" Height="30" Margin="0,0,8,0" Click="RevertButton_Click"/>
                <Border Width="12" Background="Transparent"/>
                <TextBlock Text="접두사:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="CheckinPrefixTextBox" Width="180" Height="30" VerticalContentAlignment="Center" Margin="0,0,8,0" TextChanged="CheckinPrefixTextBox_TextChanged"/>
                <TextBlock Text="체크인 설명:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="CheckinDescriptionTextBox" Width="300" Height="30" VerticalContentAlignment="Center"/>
            </StackPanel>
        </DockPanel>

        <DataGrid x:Name="SheetDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionUnit="FullRow"
                  SelectionMode="Single"
                  PreviewMouseRightButtonDown="SheetDataGrid_PreviewMouseRightButtonDown"
                  LoadingRow="SheetDataGrid_LoadingRow">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="값복사" Click="CopyEditorCellValueMenuItem_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="EditorStatusTextBlock" Grid.Column="0" Foreground="#444"/>
            <TextBlock x:Name="CheckoutStateTextBlock" Grid.Column="1" Foreground="#666"/>
        </Grid>
    </Grid>
</Window>
