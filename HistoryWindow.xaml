<Window x:Class="ExcelFinder.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Perforce File History"
        Height="720"
        Width="1180"
        MinHeight="520"
        MinWidth="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderTextBlock" Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap"/>

        <DataGrid x:Name="HistoryDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  HeadersVisibility="Column"
                  MinRowHeight="24"
                  RowHeaderWidth="0"
                  PreviewMouseRightButtonDown="HistoryDataGrid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Diff" Click="OpenDiffMenuItem_Click"/>
                    <MenuItem Header="Download" Click="DownloadMenuItem_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Revision" Binding="{Binding Revision}" Width="70"/>
                <DataGridTextColumn Header="Changelist" Binding="{Binding Changelist}" Width="90"/>
                <DataGridTextColumn Header="Date Submitted" Binding="{Binding DateSubmitted}" Width="140"/>
                <DataGridTextColumn Header="Submitted By" Binding="{Binding SubmittedBy}" Width="120"/>
                <DataGridTextColumn Header="FileType" Binding="{Binding FileType}" Width="110"/>
                <DataGridTemplateColumn Header="Description" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Description}"
                                       TextWrapping="Wrap"
                                       Margin="2,0,2,0"
                                       ToolTip="{Binding Description}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="StatusTextBlock" Grid.Row="2" Foreground="#444"/>
    </Grid>
</Window>
